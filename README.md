khetha-django
=============

Quickstart
----------

Start PostgreSQL:

```
docker-compose up
```

Run the tests:

```
tox
```

Updating dependencies
---------------------

Use Pipenv, and run [requirements-update.sh] after any Pipfile.lock update.
